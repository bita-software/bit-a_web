/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://bit-a.com',
  generateRobotsTxt: true,
  changefreq: 'monthly',
  priority: 0.7,
  sitemapSize: 5000,
};

export default config;
